language: generic
sudo: required
services:
  - docker
env:
  global:
    - SHA=$(git rev-parse HEAD)
before_install:
  - pip install --upgrade pip
  - pip install awscli
  - export PATH=$PATH:$HOME/.local/bin
deploy:
  provider: script
  script: bash deploy.sh
  on:
    branch: master
